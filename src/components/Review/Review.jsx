import { MapContainer } from "react-leaflet";
import styled from "styled-components";

export const ReviewMap = styled(MapContainer)`
  display: flex;
  width: 85vw;
  height: 60vh;
  padding-top: 10vh;

`